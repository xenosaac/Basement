# Database — Vercel Postgres auto-injects POSTGRES_URL
# For local dev, use DATABASE_URL with local PostgreSQL
DATABASE_URL="postgresql://youruser@localhost:5432/basement"
# POSTGRES_URL is auto-set by Vercel when you connect a Postgres store

# Admin + Cron secrets (set any random string)
ADMIN_SECRET="change-me"
CRON_SECRET="change-me"

# JWT for session cookies (set any random string, 32+ chars)
JWT_SECRET="change-me-to-something-long-and-random"

# Server wallet private key for faucet (optional)
# FAUCET_PRIVATE_KEY="0x..."

# Aptos T4 — module / FA addresses (filled in Session B after Move deploy)
BASEMENT_MODULE_ADDRESS=0x_STUB_REPLACE_IN_SESSION_B
VIRTUAL_USD_METADATA_ADDRESS=0x_STUB_REPLACE_IN_SESSION_B
ADMIN_ADDRESS=0x_STUB_REPLACE_IN_SESSION_B

# Pyth Hermes (defaults hardcoded; override only for private relay)
PYTH_HERMES_URL=https://hermes.pyth.network
PYTH_BTC_FEED_ID=0x_STUB_REPLACE_IN_SESSION_B
PYTH_ETH_FEED_ID=0x_STUB_REPLACE_IN_SESSION_B

# Admin private key — DO NOT set in Session A; wired in Session E
# APTOS_ADMIN_PRIVATE_KEY=
